# TREV 의존성.
openai==2.41.0
pydantic==2.13.4
tenacity==9.1.4
PyYAML==6.0.3
python-dotenv==1.2.2
pytest==9.0.3

# 인덱싱/검색 (D4/#20)
faiss-cpu==1.14.2
rank-bm25==0.2.2
numpy==2.4.6
# 실 e5 임베딩(E5Embedder). torch 포함 대용량 — 인덱스 빌드 시에만 필요.
sentence-transformers
