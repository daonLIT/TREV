# HAI-GPT API Gateway (OpenAI 호환). 실제 키를 .env에 복사해 채운다(.env는 .gitignore).
# 게이트웨이는 하나의 키·하나의 Base URL로 여러 제공사 모델을 OpenAI SDK로 호출한다.
HAI_GPT_API_KEY=your_api_key_here
HAI_GPT_BASE_URL=https://factchat-cloud.mindlogic.ai/v1/gateway
